import InterviewEditor from "@/components/editor";
import { EDITOR_DATA } from "@/data/editor-data";

const EditorPage = () => {
  return <InterviewEditor defaultData={EDITOR_DATA} />;
};

export default EditorPage;
